// import CustomForm from '../login/Form';

import LoginForm from '../login/LoginForm';
const WagonPage = props => {
    // const {title, isRegister} = props
    return <LoginForm {...props} />;
};
export default WagonPage;
